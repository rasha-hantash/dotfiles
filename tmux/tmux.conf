# --- Prefix ---
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# --- General ---
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"
set -g extended-keys always
set -as terminal-features 'xterm*:extkeys'

# --- Pane focus ---
set -g pane-border-style 'fg=#181825'
set -g pane-active-border-style 'fg=#f9e2af'
set -g window-style 'bg=#181825'
set -g window-active-style 'bg=#1e1e2e'

# --- Status bar (top, minimal) ---
set -g status-position top
set -g status-style 'bg=#1e1e2e fg=#cdd6f4'
set -g status-left '#[fg=#b4befe,bold] ccs #[fg=#45475a,nobold]│ '
set -g status-left-length 20
set -g status-right '#[fg=#6c7086]%H:%M '
set -g status-justify left

# Window tabs in status bar
setw -g window-status-format '  #W '
setw -g window-status-current-format '  #W '
setw -g window-status-current-style 'bg=#fab387 fg=#1e1e2e bold'
setw -g window-status-style 'fg=#6c7086'
setw -g window-status-separator ''

# Messages
set -g message-style 'bg=#313244 fg=#cdd6f4'
set -g message-command-style 'bg=#313244 fg=#cdd6f4'

# --- Pane focus hotkeys ---
# Layout: .1 = claude (top), .2 = terminal (bottom-left), .3 = sidebar (bottom-right)
# Panes auto-respawn via remain-on-exit + pane-died hook (set in ccs script)
bind f select-pane -t :.1
bind s select-pane -t :.3
bind m select-pane -t :.2
bind d detach-client

# --- Layout ---
# Created explicitly by ccs start/new (not via hook, to ensure consistent sizing)
