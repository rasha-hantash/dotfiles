# Catppuccin Mocha base — matches tmux unfocused bg so borders disappear
background = 1e1e2e
foreground = cdd6f4

# Use Option as Meta (enables Alt+ shortcuts in Claude Code, vim, etc.)
macos-option-as-alt = true

# CCS pane focus shortcuts
# Sends tmux prefix (Ctrl+A = \x01) followed by the pane key
keybind = cmd+j=text:\x01f
keybind = cmd+p=text:\x01s
keybind = cmd+m=text:\x01m
